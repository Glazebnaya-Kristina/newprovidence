mixin security(mods)
  -
  section.security(class=allMods)&attributes(attributes)
    .security__inner
      .security__wrapper.block-info
        h2.block-info__title Your day #[br] is protected
        p.block-info__text.
          There have not been any since we have lived here, said my mother.
        p.block-info__text.
          We thought - Mr. Copperfield thought - it was quite a large rookery; but the nests were very old ones, and the birds have deserted them a long while.
        .security__buttons.block-info__row
          button(type="button").block-info__btn.btn.btn--blue.btn--icon.btn--icon-arrow Try to hack us
          button(type="button").block-info__btn.btn.btn--border.btn--border-grey.btn--middle Learn more